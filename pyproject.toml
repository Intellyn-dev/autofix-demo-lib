[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "autofix-demo-lib"
version = "2.0.0"
description = "Demo user management library (AutoFix live demo)"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
